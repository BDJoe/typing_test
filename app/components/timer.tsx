import React, { useRef, useState } from "react";

const Timer = () => {
	const [timeLeft, setTimeLeft] = useState(60);
	const intervslRef = useRef(null);

	return <div>Timer</div>;
};

export default Timer;
